[project]
name = "benchpress-python"
version = "1.0.0"
description = "Official Python SDK for Benchpress Model Routing & Agent Trajectory Intelligence"
authors = [{ name = "Benchpress Team" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.7.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.4.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
